# 画面編集追跡システム

Figma のように全画面をサムネイルで俯瞰し、Claude が加えた変更を
「どの画面の・どの部分が変わったか」まで可視化するシステムです。

---

## Claude への常設指示（毎回これに従うこと）

**この指示はいつ・どんな依頼のときでも有効です。**

1. **画面(`*Page.tsx`)や共通コンポーネントを編集したら、必ず編集追跡が動いている状態にすること。**
   追跡は `.claude/settings.local.json` の hooks（`PreToolUse` / `PostToolUse`）で自動実行される。
   hooks が動いていない場合は、手動で以下を実行して記録を補完する:

   ```bash
   node .claude/track-edits.cjs <編集したファイルのパス>
   ```

2. **作業の最後に、必ず「今回どの画面が変わったか」を日本語で報告すること。**
   - 🔴 **直接編集した画面**（そのページのファイルを触った）
   - 🟠 **波及した画面**（共通コンポーネントを触った結果、見た目が変わりうる画面）
   - 波及先が多いときは件数だけでよいが、**必ず件数は伝える**。
   - 意図していない画面に波及していそうなら、その旨をはっきり指摘すること。

   確認用コマンド:

   ```bash
   node .claude/report.cjs
   ```

3. **見た目に関わる変更をしたら、ピクセル差分を撮って報告に含めること。**
   dev サーバー(`npm run dev`)が動いていれば、変更のあった画面だけ撮り直して
   変更前と自動で比較できる。

   ```bash
   node .claude/capture-screens.cjs
   ```

   - 「見た目 0%」なのにコードが変わっている → ロジックだけの変更。そう伝える。
   - 意図した箇所以外が光っていたら、**必ず指摘すること**（レイアウトのずれ等）。
   - 「○px ずれ」と出たら、変化率の数字ではなく**ずれた量**で報告する。
     8px ずらすと全ピクセルが変わるので、率だけ見ると過大に見える。
   - 「描画が安定しませんでした」と出た画面は差分が信頼できない。少し待って撮り直す。
   - 「JSエラー」と出た画面はアプリ側の不具合。撮り直しでは直らないので報告する。
   - 基準がまだ無い場合は、先に `--baseline` を撮るようユーザーに案内する。

4. **新しい `*Page.tsx` を追加した / ルーティングを変えたときは、画面マップを作り直すこと。**

   ```bash
   node .claude/generate-screen-map.cjs
   ```

5. **ユーザーが「マップを見たい」「どこ変わった？」と言ったら、ダッシュボードの開き方を案内すること。**

   ```bash
   node .claude/serve-map.cjs
   ```

6. **`.claude/` 配下の追跡用スクリプトは、依頼されない限り勝手に変更しないこと。**

---

## 使い方（人間向け）

### ダッシュボードを開く

```bash
node .claude/serve-map.cjs
```

`http://localhost:4321` が開きます。5 秒ごとに自動更新されるので、
Claude に作業を頼みながら開きっぱなしにしておくのがおすすめです。

`.claude/screen-map-viewer.html` をダブルクリックしても開けますが、
その場合は自動更新が効かないので「再読み込み」ボタンを押してください。

### 見方

**すべて / Admin / App タブ**は、画面同士のつながり（どのURLがどのURLの子か）から
自動で「一覧 → 詳細 → 編集」のような遷移図を組み立て、同じ形の機能をまとめて表示します
（似た構成の機能が多いほど、大きなセクションになります）。ノードは実スクリーンショット、
矢印は URL の親子関係（主要な遷移のみ。ボタン単位のリンクを全部は追っていません）。

**変更ありタブ**だけは今まで通り、「Admin / App」→「機能（フォルダ）ごとのセクション」→
カード一覧の順で表示します。

**Ver.1.0〜Ver.4.0 のメニュー**（検索欄の右）は、帳票が追加された順のバージョンで
画面を絞り込むフィルタです。もう一度同じボタンを押すと解除されます。
どのタブ（変更あり/すべて/Admin/App）にいても効きます。
対応表は `.claude/screen-map-viewer.html` の `VERSION_GROUPS` 定数。
新しい帳票を追加したときはここも更新すること。

どちらの表示でも、画面のタイトルは `PageTitleBar` / `AppHeader` の `title` から取った、
実際にアプリ上で表示される見出し（Figma でいう「フレーム名」）です。
その下の小さい文字はコンポーネント名（同じ見出しの画面が複数あるときの手がかり）。

| 表示 | 意味 |
| --- | --- |
| 🔴 赤枠 + 「編集」 | そのページのファイルを直接編集した |
| 🟠 橙枠 + 「波及」 | 共通コンポーネントを編集した結果、影響を受けうる画面 |
| 枠なし | 今回のセッションでは変わっていない |
| セクション見出し横の ●数字 | そのセクション内の直接編集（赤）/波及（橙）件数 |
| カード内の赤いタグ | 変わった「部分」（見出し名・セクション名） |
| `+12 −3` | 追加行数 / 削除行数 |

カードをクリックすると右からパネルが開き、

- 変わった部分（見出し・セクション単位）
- 経由した共通コンポーネント
- 実際の差分（追加/削除行）
- その画面の構成（セクションと行番号）

が確認できます。

### 実画面のスクリーンショットとピクセル差分

**1回だけ「変更前」の基準を撮っておく**（dev サーバーを起動した状態で）:

```bash
node .claude/capture-screens.cjs --baseline
```

全 258 画面（ルートを持つもの）をヘッドレス Chrome で開いて撮影します。
撮った絵はそのままダッシュボードのサムネイルになります。

所要時間の目安は **15 分前後**。最初の 1〜2 分は vite が各ルートのモジュールを
初めて変換するため 2〜3 枚/分と遅く、温まると 20 枚/分ほどに上がります。
途中で止めた場合は `--resume` で続きから撮れます。

**Claude に編集を頼んだあと**:

```bash
node .claude/capture-screens.cjs
```

変更のあった画面だけを撮り直し、変更前とピクセル単位で比較します。
ダッシュボードのカードをクリックすると **変更前 / 変更後 / 差分** を切り替えられます。

差分画像では、変わっていない所は薄いグレー、**変わった所はマゼンタ**、
変化のかたまりは赤い枠で囲まれます。「ラベルを 1 つ変えただけのつもりが、
下のカード全体がずれていた」といったことが一目で分かります。

| オプション | 意味 |
| --- | --- |
| `--baseline` | 全画面を撮って「変更前」として保存（基準の作り直し） |
| `--all` | 全画面を撮り直して比較 |
| `--only <文字列>` | 画面名/パスの部分一致で絞る |
| `--port <n>` | dev サーバーのポート（既定は自動検出） |
| `--width <n>` | ビューポート幅（既定 1280） |
| `--concurrency <n>` | 同時に開くタブ数（既定 4） |
| `--resume` | 撮影済みのぶんをとばして続きから |

**ルートパラメータについて**

`/factories/:factoryId` のような URL は `.claude/route-params.json` の値で埋めています。
このリポジトリの ID 規則（`f1` `u1` `r1` …）から自動で作ったものなので、
「この画面だけデータが出ていない」ときはこのファイルを直してください。

```json
{
  "global": { ":factoryId": "f1", ":unitId": "u1" },
  "byScreen": {
    "src/admin/features/xxx/DetailPage.tsx": { ":recordId": "r7" }
  }
}
```

**撮影の再現性について**

差分をあてにするには、同じ画面が毎回まったく同じ絵にならないといけません。
そのために次の対策を入れています。

| 対策 | 理由 |
| --- | --- |
| 時刻を `2026-09-02 09:00` に固定 | 時計が 1 秒進むだけで差分が出る |
| `Math.random` を固定シード | 乱数でレイアウトが変わる箇所がある |
| CSS アニメーション/トランジションを無効化 | 途中のフレームを掴んでしまう |
| `#root` に中身が入り DOM の変化が止まるまで待つ | 描画途中の白い絵を撮ってしまう |
| **同じ絵が 2 回連続で撮れるまで撮り直す** | 編集直後は vite の再コンパイルと HMR が走っていて、一瞬の中間状態を掴む |
| 白紙を検出して 3 回まで撮り直す | 白い基準画像は「全面が変わった」誤検出になる |
| JS エラーを記録 | 白いのが撮影ミスかアプリの不具合か区別する |

**「○px ずれ」表示について**

要素を 16px 下げると、それ以下の全ピクセルが動くので素朴な比較では
「98% が変化」と出てしまい、人間には役に立ちません。
行ごとのハッシュを取って「何 px ずれたか」を割り出し、
`16px ずれ` のように表示します。**率よりこちらを見てください。**

### 変更マークをリセットする

「ここから先の変更だけ見たい」ときに実行します。
実行時点のファイル内容が新しい基準（ベースライン）になります。

```bash
node .claude/reset-edits.cjs
```

### 変更サマリを端末で見る

```bash
node .claude/report.cjs
```

---

## 仕組み

```
Claude がファイルを編集
  │
  ├─ PreToolUse  → snapshot-before.cjs
  │                編集「直前」の中身を .claude/.baseline/ に退避
  │                （git diff だと元からあった未コミット変更が混ざるため）
  │
  └─ PostToolUse → track-edits.cjs
                   ベースラインと現在の内容を比較して
                     1. どの画面か（直接 or 共通部品経由の波及）
                     2. どの部分か（見出し・セクション単位）
                     3. 実際の差分
                   を edited_screens.json に記録
                        │
                        ▼
              screen-map-viewer.html が 5 秒ごとに読み直して表示


別系統: 実画面のスクリーンショット
  node .claude/capture-screens.cjs
    │
    ├─ Chrome をヘッドレス起動（CDP を直接叩く。npm パッケージなし）
    ├─ 時刻/乱数を固定し、アニメーションを止める
    ├─ 各画面の URL を開いてフルページ撮影 → .claude/.shots/
    └─ before と after を canvas でピクセル比較 → diff 画像 + 変化領域の矩形
                     │
                     ▼
         shots-index.json → ダッシュボードが読み込んで表示
```

### 「どの部分が変わったか」の判定

`generate-screen-map.cjs` / `track-edits.cjs` は各ページから **セクション**を抽出します。

- `function Foo()` / `const Foo = () =>` … コード単位
- `{/* 基本情報 */}` … JSX のセクションコメント
- `<h2>設定一覧</h2>` … 見出しテキスト
- `>金属探知機名<` … 日本語ラベル

変更された行番号がどのセクションの行範囲に入るかを見て、
「金属探知機名のところが 3 行変わった」のように特定します。

### 画面 ID について

画面 ID はファイルパスの SHA1 から作っています（例 `scr_5021db3ab2`）。

以前は走査順の連番（`screen_1`, `screen_2` …）でしたが、
`*Page.tsx` を 1 つ追加・削除するだけで以降の ID が全部ずれ、
**撮影済みスクリーンショットや編集記録との対応が黙って壊れました**（実際に起きました）。
パス由来なら、他のファイルが増減しても ID は動きません。

ファイルを移動・リネームしたときは ID が変わるので、その画面の
スクリーンショットだけ撮り直しになります。

### 「波及」の判定

`impact-index.json` に **共通ファイル → 影響を受ける画面ID** の逆引き表が入っています。
各ページの import を深さ 5 まで辿って作った依存グラフの逆引きです。

例:

| 共通ファイル | 影響画面数 |
| --- | --- |
| `src/admin/components/PageTitleBar.tsx` | 178 |
| `src/admin/components/Breadcrumb.tsx` | 140 |
| `src/admin/components/Pulldown.tsx` | 39 |

`Pulldown.tsx` を 1 行直すと 39 画面に波及しうる、というのが事前に分かります。

---

## ファイル構成

```
.claude/
├── generate-screen-map.cjs   # 画面マップ + 依存グラフの生成
├── track-edits.cjs           # PostToolUse: 差分を解析して記録
├── snapshot-before.cjs       # PreToolUse: 編集前の内容を退避
├── report.cjs                # 端末で変更サマリを表示
├── reset-edits.cjs           # 変更マークをリセット
├── serve-map.cjs             # ダッシュボード用ミニサーバ (:4321)
├── migrate-ids.cjs           # 旧 screen_N から新 ID への移行（通常は不要）
├── capture-screens.cjs       # 実スクリーンショット撮影 + ピクセル差分
├── screen-map-viewer.html    # ダッシュボード本体
├── lib/
│   ├── analyze.cjs           # 構造・セクション解析の共有ロジック
│   ├── cdp.cjs               # 依存なしの Chrome DevTools Protocol クライアント
│   ├── pixel-diff.cjs        # canvas を使ったピクセル比較
│   └── route-params.cjs      # :factoryId 等を実データの ID に解決
│
├── screen-map.json           # 生成物: 259 画面のメタ情報
├── impact-index.json         # 生成物: 共通ファイル → 影響画面
├── edited_screens.json       # 生成物: 今回の変更記録（git 管理外）
├── screen-data.js            # 生成物: file:// で開く用（git 管理外）
├── route-params.json         # ルートパラメータの実データ（手で直してよい）
├── shots-index.json          # 生成物: 撮影結果と差分の統計（git 管理外）
├── .shots/                   # 生成物: before/after/diff/thumb の画像（git 管理外）
├── .baseline/                # 生成物: 編集前スナップショット（git 管理外）
└── settings.local.json       # hooks 設定
```

> **注意:** このリポジトリは `package.json` に `"type": "module"` があるため、
> `.claude/` のスクリプトは **必ず `.cjs` 拡張子**にすること。
> `.js` にすると `require is not defined` で hook が黙って失敗します。

---

## トラブルシューティング

**編集したのにマークが付かない**

```bash
# 1. マップにそのファイルが登録されているか
node -e "const m=require('./.claude/screen-map.json');console.log(m.screens.some(s=>s.filePath==='src/.../XxxPage.tsx'))"

# 2. 手動で追跡を実行してみる
node .claude/track-edits.cjs src/.../XxxPage.tsx
```

**hooks が動いていない**
`.claude/settings.local.json` の `hooks` に `PreToolUse` と `PostToolUse` の
両方があるか確認してください。片方だけだと差分が取れません。

**スクリーンショットが撮れない / 真っ白**
`npm run dev` が動いているか確認してください（`--port` で明示指定もできます）。
Chrome の場所が特殊な場合は `CHROME_PATH` 環境変数で指定します。

**特定の画面だけ「データが見つかりません」と出る**
`.claude/route-params.json` のパラメータがその画面のモックデータと合っていません。
`byScreen` にその画面のパスを足して、正しい ID を指定してください。

**差分が意味もなく大量に出る**
別のセッションが同時に編集していないか確認してください。
基準を撮り直すなら `node .claude/capture-screens.cjs --baseline` です。

**画面とスクリーンショットの対応がずれている**
古い連番 ID のデータが残っています。1 回だけ移行を実行してください:

```bash
node .claude/generate-screen-map.cjs && node .claude/migrate-ids.cjs
```

**サムネイルが真っ白な画面がある**
その画面の描画が撮影に間に合っていません。`--only <画面名>` で撮り直してください。
白い基準画像は後の比較で「全面が変わった」という誤検出になるので放置しないこと。

**ダッシュボードが真っ白**
`node .claude/generate-screen-map.cjs` を実行してから
`node .claude/serve-map.cjs` で開いてください。

---

## 今後の拡張案

- [x] 実スクリーンショットのサムネイル化
- [x] 変更前後のスクリーンショット差分（ピクセル比較）
- [ ] `Stop` フックで作業終了時に自動レポート
- [ ] Git 連携（コミット単位での変更履歴）
- [ ] レスポンシブ差分（スマホ幅でも撮って比較）
